

/*
test('Deck card', () => {
    let code = 'RANDOM_CODE';
    let name = 'RANDOM_NAME';
    let deckCard = new DeckCard(code, name);

    
});

test('Deck', () => {
    
    let code = 'RANDOM_CODE';
    let name = 'RANDOM_NAME';
    let deckCard = new DeckCard(code, name);

    let deck = new Deck([deckCard]);

    deck.cards.contains(deckCard);
});
*/

